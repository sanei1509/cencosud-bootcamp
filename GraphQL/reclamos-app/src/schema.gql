# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Reclamo {
  id: ID!
  nroReclamo: Int!
  titulo: String!
  detalleDeCompra: String!
  problema: String!
}

type Query {
  """Listar todos los tickets de reclamos DB"""
  reclamos: [Reclamo!]!

  """Listar un ticket solicitado por app"""
  reclamoID(id: ID!): Reclamo!

  """Listar todos los tickets de reclamos por palabra clave"""
  reclamosPorPalabraClave(palabraClave: String!): [Reclamo!]!
}

type Mutation {
  """Crear un nuevo ticket de reclamo"""
  CrearReclamo(crearReclamoInput: CrearReclamoInput!): Reclamo!

  """Actualizar un ticket de reclamo existente"""
  updateReclamo(actualizarReclamoInput: ActualizarReclamoInput!): String!

  """Borrar un ticket de reclamo existente"""
  deleteReclamo(id: String!): Boolean!
}

input CrearReclamoInput {
  """titulo del reclamo"""
  titulo: String!

  """detalle de compra del reclamo"""
  detalleDeCompra: String!

  """problema del reclamo"""
  problema: String!
}

input ActualizarReclamoInput {
  """id del reclamo"""
  id: ID!

  """titulo del reclamo"""
  titulo: String

  """detalle de compra del reclamo"""
  detalleDeCompra: String

  """problema del reclamo"""
  problema: String
}